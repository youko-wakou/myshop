<div class="col-md-12 col-xs-12" style="margin-top: 15px;">
  <div class="col-md-6 col-xs-10 col-md-offset-3 col-xs-offset-1 bg-info text-center img-rounded"style= "padding:10px 0;">
    <p>&lt;前回送信したメッセージ&gt;</p>
    <% if current_user.supports.present? %>
  
      <% @new.each do |new| %>
        <h3>Title: [<%= new.title %>]</h3>
        <small><%= new.created_at %></small>
        <p>name: <%= current_user.name %></p>
        <div class="message" style="border: 3px dotted #ffaac0;">
          <%= new.content %>
        </div>
      <% end %>
    <% else %>
  <p>まだ送信したメッセージはありません</p>      
  </div>
</div>

    <% end %>